# Hello-World
Terminal Globe — A small Python project that converts a world map image into an ASCII-style globe and renders it as a continuously rotating animation in the terminal. It uses Pillow for image processing and RGB/coordinate distance calculations for pixel classification and globe rendering.
